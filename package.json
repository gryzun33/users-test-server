{
  "name": "users-test-server",
  "version": "1.0.0",
  "private": true,
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run start:dev --prefix backend",
    "migrate:seed": "npm run migrate:seed --prefix backend",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "install": "concurrently \"npm install --prefix frontend\" \"npm install --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
